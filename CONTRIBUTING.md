# 贡献指南

感谢你愿意改进 Taylor Swift Cinematic。提交前请先说明问题如何复现，以及变更会影响哪些页面、媒体流程或部署行为。本项目优先接受范围明确、容易审阅的改动。

## 本地开发

需要 Node.js 22.12.0 或更高版本，并使用支持仓库 pnpm-lock.yaml 格式的 pnpm。安装依赖并启动：

~~~sh
pnpm install
pnpm dev
~~~

音频和可选媒体不会随仓库发布。没有本地媒体时，空曲库提示属于预期体验。

## 提交前验证

根据改动范围运行适用命令。涉及扫描或同步时，请运行两项媒体工具测试；改动页面或构建配置时，运行类型检查、lint 和生产构建：

~~~sh
pnpm test:audio
pnpm test:music-sync
pnpm typecheck
pnpm lint
pnpm build
~~~

这些命令不会对真实 R2 执行上传或删除。不要为了验证而对线上 Bucket 运行同步或清理。提交描述应注明没有运行的检查和原因。

## 素材、凭证和隐私

- 不要提交 .env.local、.dev.vars、Cloudflare Token、Access Key、Secret、日志中的凭证或真实个人路径。
- 不要提交 audio/、.music-cache/、生成的 catalog.json、dist/、node_modules/ 或临时压缩包。
- 不要提交商业音频、歌词、艺人照片、专辑封面或截图，除非已确认有权公开再分发。
- 代码许可与第三方媒体许可是分开的。仓库没有 LICENSE 时，不要自行添加或宣称某个许可已生效。
- 如果贡献需要新的图片或音频，请在提交前提供权利来源和明确的再分发授权。

## 变更范围

保留已运行的 Catalog、同步和页面架构。涉及视觉效果时，说明真实浏览器中的验证尺寸、操作和结果；不要在缺少复现证据时修改 3D 圆柱几何或动画。与当前改动无关的重构应分开提交。

## Pull request 内容

请说明：

1. 解决的问题和用户可见变化。
2. 修改的核心文件与可能影响的发布流程。
3. 实际运行的验证命令和结果。
4. 新增媒体、许可证或部署变量的来源与权利状态。

在软件许可证由权利人确定之前，贡献流程不代表项目已授予代码再分发权。
